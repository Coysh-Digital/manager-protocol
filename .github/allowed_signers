# Keys permitted to sign releases of this repository.
#
# Published so that anybody can verify a tag before installing it, which is what invariant 17 asks
# for. To use it:
#
#   git config gpg.ssh.allowedSignersFile .github/allowed_signers
#   git tag -v v1.0.0
#
# This is trust-on-first-use: somebody who controlled this repository could replace the key as well
# as the tag. Cross-check it against the signing keys on https://github.com/timcoysh.keys, and
# against GitHub's own "Verified" badge on the tag, which is computed from keys held outside this
# repository.
tim@timcoysh.co.uk ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIMfodPg0KpfX8vkW4X/CE/LSqOUIGIy0yDgON18adwia tim@coysh.digital signing
